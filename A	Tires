#include <bits/stdc++.h>
using namespace std;
#define ll long long 
int main()
{
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string s;
    cin>>s;
    if(s[s.length()-1]=='r')
    cout<<"er";
    else
    cout<<"ist";
 
    return 0;
}
